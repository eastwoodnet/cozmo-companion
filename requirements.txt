# Core: direct WiFi connection to Cozmo (no mobile app needed)
pycozmo>=0.8.0

# Web server + WebSocket
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# Camera frames + video + face detection
Pillow>=10.0.0
# OpenCV 4.x: la 5.0 eliminó CascadeClassifier y los cascades Haar
opencv-python-headless>=4.8,<5

# Offline voice recognition (optional — app works without it)
vosk>=0.3.45
PyAudio>=0.2.14
